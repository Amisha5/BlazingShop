@model IEnumerable<Blazingshop.DomainModel.Models.Appointment>

@{
    ViewBag.Title = "GetYourAppointment";
}

<h2>GetYourAppointment</h2>

<body style="background-color:aliceblue;">
    <div class="content px-4">


        @if (Model.Count() == 0)
        {
            <h2>No Appointment found</h2>
        }
        else
        {
            <div class="container border p-3">
                <div class="row">
                    <div class="col-5">
                        <h3 class="text-info">Appointment List</h3>
                    </div>
                    <div class="col-3">
                    </div>
                </div>
                <div class="row pt-4">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>Name</th>

                                <th>Email</th>
                                <th>Phone</th>
                                <th>Product Name</th>
                                <th>Date</th>
                                <th>Confirmed</th>
                                
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>


                                    <td>@item.CustomerName</td>
                                    <td>@item.CustomerEmail</td>
                                    <td>@item.CustPhoneNumber</td>
                                    <td>@item.Product.ProductName</td>
                                    <td>
                                        @item.AppointmentDate
                                    </td>
                                    <td>
                                        @if (item.IsConfirmed == false)
                                        {<input type="checkbox" class="form-control" disabled />}
                                        else
                                        { <input type="checkBox" checked class="form-control" disabled />}
                                    </td>
                                </tr>

                            }
                        </tbody>
                    </table>
                </div>
            </div>
        }
    </div>

</body>